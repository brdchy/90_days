# Веб-платформа "90 дней - 10 целей"

Полноценная веб-платформа для управления и отслеживания прогресса игры.

## 🚀 Быстрый старт

### Предварительные требования

- Node.js 18+ и npm
- Python 3.11+
- Запущенный Telegram бот (для работы API)

### Установка и запуск

1. **Установите зависимости для фронтенда:**

```bash
cd web
npm install
```

2. **Установите зависимости для API:**

```bash
pip install -r requirements.txt
```

3. **Запустите API сервер:**

```bash
# В корне проекта
python -m uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

4. **Запустите фронтенд:**

```bash
# В папке web
npm run dev
```

Откройте http://localhost:3000 в браузере.

## 📁 Структура проекта

```
90_days/
├── api/                  # FastAPI backend
│   └── main.py          # API сервер
├── web/                 # React frontend
│   ├── src/
│   │   ├── api/         # API клиент
│   │   ├── components/  # React компоненты
│   │   ├── pages/       # Страницы приложения
│   │   │   ├── admin/   # Админ-панель
│   │   ├── stores/      # Zustand хранилища
│   │   └── App.jsx      # Главный компонент
│   └── package.json
└── requirements.txt     # Python зависимости
```

## 🎯 Функциональность

### Публичная часть

- **Главная страница** — общая статистика игры
- **Участники** — список всех участников с их прогрессом
- **Детальная страница участника** — полная статистика по целям, графики прогресса
- **Отчеты** — все ежедневные отчеты участников, сгруппированные по дням
- **Моя статистика** — персональная статистика (требует настройки авторизации)

### Админ-панель

Доступ: http://localhost:3000/admin/login
- Логин: `admin`
- Пароль: `admin`

**Возможности:**
- 📊 **Дашборд** — общая статистика игры, процент отчетов
- 👥 **Управление участниками** — просмотр всех участников, их статусов
- ⚙️ **Настройки бота** — настройка времени напоминаний и исключения участников
- 📈 **Мониторинг** — отслеживание активности участников

## 🛠 Технологии

### Frontend
- **React 18** — UI библиотека
- **Vite** — сборщик и dev сервер
- **TailwindCSS** — стилизация
- **React Router** — маршрутизация
- **TanStack Query** — управление состоянием сервера
- **Zustand** — управление состоянием клиента
- **Recharts** — графики и визуализация
- **Lucide React** — иконки
- **Axios** — HTTP клиент

### Backend
- **FastAPI** — современный Python веб-фреймворк
- **Uvicorn** — ASGI сервер
- **HTTP Basic Auth** — аутентификация для админки

## 📝 API Endpoints

### Публичные

- `GET /api/participants` — список участников
- `GET /api/participants/{user_id}` — информация об участнике
- `GET /api/reports?user_id={id}` — отчеты (с фильтром по user_id)
- `GET /api/stats/{user_id}` — статистика участника
- `GET /api/current-day` — текущий день игры

### Админские (требуют Basic Auth)

- `GET /api/admin/stats` — общая статистика
- `GET /api/admin/settings` — настройки бота
- `PUT /api/admin/settings` — обновить настройки
- `POST /api/admin/remind` — отправить напоминания

## 🎨 Особенности UI/UX

- ✅ Адаптивный дизайн (mobile-first)
- ✅ Современный и чистый интерфейс
- ✅ Интерактивные графики прогресса
- ✅ Удобная навигация
- ✅ Быстрая загрузка данных
- ✅ Обработка ошибок и состояний загрузки
- ✅ Цветовая индикация статусов

## 🔒 Безопасность

- Админ-панель защищена HTTP Basic Auth
- В продакшене обязательно измените пароль в `api/main.py`
- CORS настроен для работы с фронтендом
- Валидация данных через Pydantic

## 📦 Сборка для продакшена

### Frontend

```bash
cd web
npm run build
```

Собранные файлы будут в `web/dist`. Их можно раздавать через nginx или другой веб-сервер.

### API

```bash
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

Рекомендуется использовать процесс-менеджер (systemd, supervisor) или Docker.

## 🌐 Деплой

1. Разверните API на сервере (порт 8000)
2. Разверните собранный фронтенд на веб-сервере (nginx)
3. Настройте проксирование `/api/*` на API сервер
4. Обновите `VITE_API_URL` в `.env` или переменных окружения

## 💡 Разработка

### Добавление новых страниц

1. Создайте компонент в `web/src/pages/`
2. Добавьте маршрут в `web/src/App.jsx`
3. Добавьте ссылку в навигацию при необходимости

### Добавление новых API endpoints

1. Добавьте endpoint в `api/main.py`
2. Добавьте метод в `web/src/api/client.js`
3. Используйте в компонентах через `useQuery` или `useMutation`

## 📞 Поддержка

При возникновении проблем проверьте:
- Запущен ли API сервер
- Правильно ли настроен CORS
- Доступны ли данные в Excel файле
- Логи в консоли браузера и терминале

