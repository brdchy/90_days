# 🚀 Быстрый старт веб-платформы

## Шаг 1: Установка зависимостей

### Backend (API)
```bash
pip install -r requirements.txt
```

### Frontend
```bash
cd web
npm install
```

## Шаг 2: Запуск

### Запуск API (терминал 1)
```bash
python run_api.py
```
Или:
```bash
python -m uvicorn api.main:app --reload --host 0.0.0.0 --port 8000
```

API будет доступно на http://localhost:8000
Документация API: http://localhost:8000/docs

### Запуск Frontend (терминал 2)
```bash
cd web
npm run dev
```

Frontend будет доступен на http://localhost:3000

## Шаг 3: Использование

1. Откройте http://localhost:3000 в браузере
2. Просматривайте участников, отчеты и статистику
3. Для доступа к админ-панели: http://localhost:3000/admin/login
   - Логин: `admin`
   - Пароль: `admin`

## ⚠️ Важно

- Убедитесь, что Telegram бот запущен и работает
- API должен иметь доступ к Excel файлу на Яндекс.Диске
- В продакшене обязательно измените пароль админа!

