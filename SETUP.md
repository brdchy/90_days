# 📋 Инструкция по настройке веб-платформы

## Установка и настройка

### 1. Backend (FastAPI)

```bash
# Установка зависимостей
pip install fastapi uvicorn[standard] python-multipart

# Или все зависимости сразу
pip install -r requirements.txt
```

### 2. Frontend (React)

```bash
cd web

# Установка зависимостей (первый запуск)
npm install

# Создайте файл .env (опционально)
echo "VITE_API_URL=http://localhost:8000" > .env
```

## Запуск

### Вариант 1: Отдельные терминалы

**Терминал 1 - API:**
```bash
python run_api.py
```

**Терминал 2 - Frontend:**
```bash
cd web
npm run dev
```

### Вариант 2: Используя скрипты

**Windows PowerShell:**
```powershell
# Запуск API в фоне
Start-Process python -ArgumentList "run_api.py"

# Запуск Frontend
cd web
npm run dev
```

**Linux/Mac:**
```bash
# Запуск API в фоне
python run_api.py &

# Запуск Frontend
cd web
npm run dev
```

## Проверка работы

1. **API должен быть доступен:**
   - http://localhost:8000
   - http://localhost:8000/docs (Swagger документация)

2. **Frontend должен быть доступен:**
   - http://localhost:3000

3. **Админ-панель:**
   - http://localhost:3000/admin/login
   - Логин: `admin`
   - Пароль: `admin`

## Возможные проблемы

### Проблема: API не запускается

**Решение:**
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте, что порт 8000 свободен
- Убедитесь, что файл `.env` существует и содержит токены

### Проблема: Frontend не подключается к API

**Решение:**
- Убедитесь, что API запущен на порту 8000
- Проверьте настройки CORS в `api/main.py`
- Проверьте файл `web/.env` или переменную `VITE_API_URL`

### Проблема: Ошибки импорта в API

**Решение:**
- Убедитесь, что запускаете из корневой директории проекта
- Проверьте, что все модули доступны (services, handlers, config_reader)

### Проблема: Frontend показывает ошибки

**Решение:**
- Проверьте консоль браузера (F12)
- Убедитесь, что установлены все зависимости: `npm install`
- Проверьте, что API доступен и отвечает

## Структура файлов

```
90_days/
├── api/
│   └── main.py              # FastAPI сервер
├── web/
│   ├── src/
│   │   ├── api/             # API клиент
│   │   ├── components/      # React компоненты
│   │   ├── pages/            # Страницы
│   │   │   └── admin/        # Админ-панель
│   │   ├── stores/           # Zustand хранилища
│   │   └── App.jsx           # Главный компонент
│   ├── package.json
│   └── vite.config.js
├── run_api.py                # Скрипт запуска API
└── requirements.txt
```

## Следующие шаги

1. ✅ Установите зависимости
2. ✅ Запустите API и Frontend
3. ✅ Проверьте работу админ-панели
4. ✅ Настройте переменные окружения для продакшена
5. ✅ Измените пароль админа в `api/main.py`

